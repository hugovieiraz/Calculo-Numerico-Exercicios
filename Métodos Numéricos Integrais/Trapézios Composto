import numpy as np

a = 0
b = 1
n = 9

h = (b - a) / (n - 1)

xi = a + h
soma = 0

def f(x):
    return x**3

for i in range(1, n - 1):
    if i % 2 == 0:
        soma += 2 * f(xi)
    else:
        soma += 4 * f(xi)
    
    xi += h

I = (h / 3) * (f(a) + soma + f(b))

print(I)